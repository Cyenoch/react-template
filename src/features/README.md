Example:

```
src/
  features/
    some-module/
      functions/ - server functions
      hooks/ - hooks
      store/ - zustand
      components/ - components
      schema/ - zod schemas
      types/ - typescript interfaces
      xxx-view.tsx - view
```
